---
phase: 08-evaluation-and-docs
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/object_search/eval/charts.py
  - src/object_search/cli.py
  - docs/benchmark/*.png
  - docs/benchmark/results.md
  - README.md
  - docs/methods/ncc.md
  - docs/methods/sparse-geo.md
  - docs/methods/dino-dense.md
  - docs/methods/propose-retrieve.md
  - docs/ROBUSTNESS-BACKLOG.md
  - docs/MILESTONE-2.md
  - tests/test_charts.py
autonomous: true
requirements: [EVAL-06, DOC-03, DOC-04, DOC-05, DOC-06]
user_setup: []

must_haves:
  truths:
    - "benchmark charts are committed and regenerate byte-identically"
    - "the README shows sample runs for all four methods side by side"
    - "each method doc has algorithm, explicit pre/post-processing, config reference, failure modes, backlog"
    - "the robustness backlog aggregates every method plus lattice verification and deferred methods"
    - "MILESTONE-2.md points at the real seams that shipped"
    - "the honest-limitations section states INFRA-07, MobileSAM, AGPL, non-commercial weights, and any underperformance"
  artifacts:
    - src/object_search/eval/charts.py
    - README.md
    - docs/MILESTONE-2.md
  key_links:
    - "charts render headlessly and suppress the matplotlib PNG timestamp for byte-identical output"
    - "the README sample grid is produced by the registry-iterating renderer"
---

<objective>
Render the committed benchmark charts, write the README and the four method docs, aggregate the
robustness backlog, specify Milestone 2, and state the project's limitations honestly.

Output: charts, README, `docs/methods/*`, `docs/ROBUSTNESS-BACKLOG.md`, `docs/MILESTONE-2.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/08-evaluation-and-docs/08-CONTEXT.md
@.claude/CLAUDE.md
@.planning/STATE.md
@.planning/IDEA.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: charts.py — committed, deterministic benchmark visuals</name>
  <files>src/object_search/eval/charts.py, src/object_search/cli.py, docs/benchmark/results.md, tests/test_charts.py</files>
  <action>
`charts.py` — read `docs/benchmark/results.json`, render (matplotlib, **Agg backend**): a
per-method precision/recall/F1/AP bar chart; a per-slice chart showing the NCC-vs-sparse-geo
crossover across instance count / scale; a latency-by-canvas-size chart (the EVAL-19 scaling
story); a thumbs-up chart with **Wilson intervals** where ratings exist. Also emit
`docs/benchmark/results.md` (the tables).

**Deterministic render:** matplotlib embeds a creation timestamp in PNG metadata by default —
suppress it (`metadata={"Software": None}` / set a fixed value, or post-process) so two renders
are byte-identical. Wire `pixi run bench-charts` (or extend the cli) to regenerate.

Test: render twice into temp dirs and assert byte-identical PNGs (EVAL-06 determinism).
  </action>
  <verify>pixi run test tests/test_charts.py -v</verify>
  <acceptance_criteria>
    - charts render headlessly (Agg) and suppress the PNG timestamp
    - a test asserts two renders are byte-identical
    - a latency-by-canvas-size chart exists (the EVAL-19 scaling story)
  </acceptance_criteria>
  <done>Committed charts regenerate identically.</done>
</task>

<task type="auto">
  <name>Task 2: README with side-by-side sample runs</name>
  <files>README.md</files>
  <action>
Replace the interim README. Structure: what it is; quickstart (`pixi install`,
`pixi run fetch-models`, `pixi run serve`); the four methods table; a **side-by-side sample-run
grid for all four methods** (DOC-03) — produced by the Phase 2 renderer which iterates the
registry, so all four appear with no per-method code (verify that by regenerating); the benchmark
summary with the committed charts; the honest-limitations section (Task 5 writes it, link it);
project layout; development commands. Do not claim anything is implemented that is not.
  </action>
  <verify>grep -q "docs/samples/propose-retrieve" README.md</verify>
  <acceptance_criteria>
    - the README shows sample runs for all four methods side by side
    - it links the benchmark charts and the limitations section
  </acceptance_criteria>
  <done>The README shows the whole project at a glance, honestly.</done>
</task>

<task type="auto">
  <name>Task 3: Per-method docs completeness pass (DOC-04)</name>
  <files>docs/methods/ncc.md, docs/methods/sparse-geo.md, docs/methods/dino-dense.md, docs/methods/propose-retrieve.md</files>
  <action>
Ensure each of the four has: algorithm (mirroring the module's numbered steps), **explicit
pre/post-processing** (exact numbers, not "standard normalization"), a config reference
**generated from the JSON Schema** (so it cannot drift), known failure modes, and the ROBUSTNESS
BACKLOG mirrored from the module docstring. Fill any gaps left by earlier phases. Add a small
script or check that regenerates the config-reference section from `config_model.model_json_schema()`
so it stays in sync.
  </action>
  <verify>for m in ncc sparse-geo dino-dense propose-retrieve; do grep -q "Pre-processing" docs/methods/$m.md || echo "MISSING $m"; done</verify>
  <acceptance_criteria>
    - all four method docs have explicit pre/post-processing and a schema-derived config reference
    - each mirrors its module's ROBUSTNESS BACKLOG
  </acceptance_criteria>
  <done>All four method docs are complete and mirror their modules.</done>
</task>

<task type="auto">
  <name>Task 4: docs/ROBUSTNESS-BACKLOG.md and docs/MILESTONE-2.md</name>
  <files>docs/ROBUSTNESS-BACKLOG.md, docs/MILESTONE-2.md</files>
  <action>
`docs/ROBUSTNESS-BACKLOG.md` (DOC-05) — aggregate every method's backlog plus the cross-cutting
items: **lattice verification** (documented, not built — likely the single highest-leverage item
for shelf/PCB/tile images, since fitting the lattice post-detection recovers misses and kills
false positives more effectively than tuning the detector), and the deferred **Method 4** and
**Method 6** with their reasoning intact.

`docs/MILESTONE-2.md` (DOC-06) — the marker-conditioned region proposal feature: find every marker
instance by reusing any Milestone 1 method; estimate each marker's reference point and orientation
(arrow tip + direction, or centroid with no direction for a symmetric marker — from PCA on the
mask, or recovered from the similarity/affine transform Method 2 already fits); **reuse Method 5's
`propose()` stage directly**; score proposals by distance from the reference point, alignment with
the marker's direction, objectness, and a size prior. State what it reuses vs what is new,
pointing at the **actual seams that shipped**: the `exploration` column, the registry,
`propose()`/`embed_regions()`, and the UI mode selector.
  </action>
  <verify>test -f docs/MILESTONE-2.md &amp;&amp; grep -q "lattice" docs/ROBUSTNESS-BACKLOG.md</verify>
  <acceptance_criteria>
    - the backlog aggregates all four methods plus lattice verification and deferred Methods 4 and 6
    - MILESTONE-2.md points at the real seams (exploration column, registry, propose/embed_regions, UI mode)
  </acceptance_criteria>
  <done>Backlog and Milestone 2 spec are complete.</done>
</task>

<task type="auto">
  <name>Task 5: The honest-limitations section</name>
  <files>docs/LIMITATIONS.md, README.md</files>
  <action>
`docs/LIMITATIONS.md` — state, without softening:
  - **INFRA-07 partial** — branch protection unavailable on a free private repo
    (`403 Upgrade to GitHub Pro or make this repository public`); CI runs on every PR but
    server-side enforcement does not exist.
  - **MobileSAM did not ship** as a working backend, and why (one-prompt-per-call decoder →
    ~1024 calls + a ported mask generator).
  - **FastSAM AGPL-3.0** — the exported `.onnx` embeds the licence; publishing the repo or
    exposing the API would fire §13.
  - **SuperPoint weights are non-commercial research-only.**
  - **Any method that underperforms in the benchmark is reported as underperforming** — read
    `docs/benchmark/results.md` and state the real findings, including the NCC-vs-sparse-geo
    crossover. A flattering benchmark would defeat the project's entire purpose.
Link it from the README.
  </action>
  <verify>grep -q "INFRA-07" docs/LIMITATIONS.md &amp;&amp; grep -q "AGPL" docs/LIMITATIONS.md</verify>
  <acceptance_criteria>
    - LIMITATIONS.md states INFRA-07, MobileSAM, AGPL, non-commercial weights, and real benchmark findings
    - the README links it
  </acceptance_criteria>
  <done>The project's limitations are stated plainly.</done>
</task>

</tasks>

<verification>
- [ ] lint/format-check/typecheck/test green, coverage ≥80%
- [ ] Charts committed and byte-identical on re-render
- [ ] README shows all four methods side by side
- [ ] Four method docs complete with explicit pre/post-processing
- [ ] Robustness backlog + Milestone 2 spec complete
- [ ] Limitations stated honestly
</verification>

<success_criteria>
- Phase 8 success criteria 2, 6 met; the project is readable cold and honest about its limits
</success_criteria>

<output>
After completion, create `.planning/phases/08-evaluation-and-docs/08-02-SUMMARY.md`
</output>
