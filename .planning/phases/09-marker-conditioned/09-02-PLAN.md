---
phase: 09-marker-conditioned
plan: 02
type: execute
wave: 2
depends_on: ["09-01"]
files_modified:
  - frontend/js/main.js
  - frontend/js/explorations.js
  - frontend/js/overlay.js
  - frontend/index.html
  - frontend/css/app.css
  - src/object_search/cli.py
  - src/object_search/samples.py
  - assets/demo/markers/*
  - assets/demo/LICENSES.md
  - docs/explorations/marker-conditioned.md
  - docs/MILESTONE-2.md
  - README.md
  - tests/test_marker_samples.py
autonomous: false
requirements: [M2-05]
user_setup: []

must_haves:
  truths:
    - "The exploration selector offers marker-conditioned, and choosing it drives the config form from that exploration's JSON schema"
    - "The results overlay draws each marker, its direction arrow, and the chosen proposal"
    - "Committed marker sample runs regenerate from one CLI command"
    - "docs/MILESTONE-2.md status flips from specified to built, pointing at the real code"
  artifacts:
    - frontend/js/explorations.js
    - docs/explorations/marker-conditioned.md
    - assets/demo/markers
  key_links:
    - "the exploration selector switches the config form source from /methods to /explorations"
    - "the marker sample runs are produced by the registry-iterating renderer"
---

<objective>
Wire the second exploration mode into the UI, commit marker demo assets and sample runs, and finish
the docs — flipping `docs/MILESTONE-2.md` from "specified" to "built" with references to the code
that now exists. Human-verified because "pick the marker mode, draw a marker, see the pointed-at
objects" is a UI flow only a person can confirm.

Output: the marker UI mode, `assets/demo/markers/`, `docs/explorations/marker-conditioned.md`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/09-marker-conditioned/09-CONTEXT.md
@.claude/CLAUDE.md
@src/object_search/explorations/marker_conditioned.py
@frontend/js/main.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: UI second exploration mode, schema-driven from /explorations</name>
  <files>frontend/js/explorations.js, frontend/js/main.js, frontend/index.html, frontend/js/overlay.js, frontend/css/app.css</files>
  <read_first>frontend/js/main.js, frontend/js/form.js</read_first>
  <action>
Populate the existing `#exploration` selector from `GET /explorations` (it currently has the one
option). On change, the config form is rebuilt from that **exploration's** JSON Schema (reuse
`form.js` `buildForm`) instead of the method's — so both explorations are schema-driven and no
exploration name is written in `frontend/`.

For `marker-conditioned`, the exploration config includes `marker_method` (a select) and its nested
config plus the scoring weights — all from the schema. The draw interaction is unchanged: the user
draws a box around the marker. On Search, POST `/search` with `exploration` set and the exploration
config; the response is the same `SearchResult` shape.

`overlay.js` — when the diagnostics carry per-marker reference points + directions, draw each
marker box, a short **direction arrow** from its reference point, and the chosen proposal box
linked to it (a thin connector). The proposal set remains a toggleable diagnostics layer. Keep it
presence-driven (no exploration/method name in the overlay).

Keep logic-bearing JS in small pure functions; there is no JS test runner (npm is disallowed), so
the Python side covers the round trip.
  </action>
  <verify>grep -q "explorations" frontend/js/explorations.js &amp;&amp; grep -Lc "marker-conditioned" frontend/js/*.js</verify>
  <acceptance_criteria>
    - the exploration selector is populated from GET /explorations
    - selecting an exploration rebuilds the config form from that exploration's schema
    - grep of frontend/ finds no exploration name literal used for dispatch
    - the overlay draws marker direction arrows and the chosen proposal when those diagnostics are present
  </acceptance_criteria>
  <done>The app hosts two explorations, both schema-driven.</done>
</task>

<task type="auto">
  <name>Task 2: Marker demo assets and sample runs</name>
  <files>src/object_search/cli.py, src/object_search/samples.py, assets/demo/markers/*, assets/demo/LICENSES.md, tests/test_marker_samples.py</files>
  <action>
Add `pixi run markers` (a `cli.py` command) generating the `MARKER_DEMO_SPECS` set into
`assets/demo/markers/` with `.markers.json` sidecars (exact tip/direction/target GT). Commit the
PNGs (respect the 2 MB pre-commit limit; they are small synthetic images).

Extend the sample renderer / `SAMPLE_MANIFEST` with a marker-exploration entry: a fixed marker
exemplar box per marker demo image, run through the `marker-conditioned` exploration, rendered under
`docs/samples/marker-conditioned/` with the marker boxes, direction arrows, and chosen proposals
drawn. Regenerable by one command; **byte-identical** on re-render (a test asserts it). Because the
renderer iterates the registry (methods and now explorations), verify the marker exploration appears
with minimal renderer change — extend the renderer to also iterate `list_explorations()` if needed,
documenting that as the exploration analogue of the method loop.

`assets/demo/LICENSES.md` — account for the new synthetic marker images (generated by this repo's
own generator, regenerable from the seed).

Tests: `pixi run markers` writes images + sidecars; the marker sample render is byte-identical on
two runs; the marker exploration's sample output exists.
  </action>
  <verify>pixi run markers &amp;&amp; pixi run samples &amp;&amp; pixi run test tests/test_marker_samples.py -v</verify>
  <acceptance_criteria>
    - pixi run markers writes marker PNGs + .markers.json sidecars under assets/demo/markers/
    - docs/samples/marker-conditioned/ is populated
    - a test asserts two marker-sample renders are byte-identical
    - LICENSES.md accounts for the marker images
  </acceptance_criteria>
  <done>Committed marker sample runs exist and regenerate deterministically.</done>
</task>

<task type="auto">
  <name>Task 3: Docs — marker-conditioned page, MILESTONE-2 status, README</name>
  <files>docs/explorations/marker-conditioned.md, docs/MILESTONE-2.md, README.md, docs/ROBUSTNESS-BACKLOG.md</files>
  <action>
`docs/explorations/marker-conditioned.md` — mirror the module's numbered steps: the pipeline,
**explicit pre/post-processing** (marker crop handling; how orientation is recovered both ways; the
scoring formula with each weight's effect), the config reference from the JSON Schema, known failure
modes (180° ambiguity fallback; markers the finding-method misses; proposal over-segmentation), and
the ROBUSTNESS BACKLOG (learned marker/keypoint tip detection; per-proposal appearance matching via
`embed_regions`; multi-marker disambiguation when arrows cross; a dedicated marker detector).

`docs/MILESTONE-2.md` — flip the status line from "specified, not built" to **"built"**; keep the
seam table but update each row's "How Milestone 2 uses it" to past tense pointing at the shipped
files (`explorations/marker_conditioned.py`, `explorations/markers.py`, `explorations/registry.py`,
`routes_explorations.py`, the UI mode). Add a short "What actually shipped vs the spec" note if the
implementation deviated.

`README.md` — add a Milestone 2 subsection with a marker sample run and a one-line description; note
that explorations are now a registry-level concept with two members.

`docs/ROBUSTNESS-BACKLOG.md` — append a marker-conditioned section.
  </action>
  <verify>grep -q "built" docs/MILESTONE-2.md &amp;&amp; grep -q "ROBUSTNESS BACKLOG" docs/explorations/marker-conditioned.md</verify>
  <acceptance_criteria>
    - docs/explorations/marker-conditioned.md has explicit pre/post-processing and a ROBUSTNESS BACKLOG
    - docs/MILESTONE-2.md status reads built and references the shipped files
    - README shows a marker sample run
  </acceptance_criteria>
  <done>The docs reflect a shipped, not specified, feature.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The marker-conditioned exploration in the app (pixi run serve), models fetched.</what-built>
  <how-to-verify>Pick the "Marker-conditioned" exploration above the method selector, confirm the config form changes to the exploration's fields (marker method + scoring weights), draw a box around one arrow on a marker demo image, run Search, and confirm: every arrow instance is found, each shows a direction arrow, and the object each arrow points at is boxed as the chosen proposal.</how-to-verify>
  <resume-signal>Type "approved" or describe what was wrong.</resume-signal>
</task>

</tasks>

<verification>
- [ ] lint/format-check/typecheck/test green, coverage ≥80%
- [ ] The exploration selector drives the form from /explorations
- [ ] Marker sample runs committed and byte-identical on re-render
- [ ] MILESTONE-2.md flipped to built; marker-conditioned doc complete
- [ ] Human-verify: the full marker→pointed-object flow works in the UI
</verification>

<success_criteria>
- Milestone 2 is usable end to end from the browser and documented as built
- Explorations are demonstrably a registry-level concept with two members
</success_criteria>

<output>
After completion, create `.planning/phases/09-marker-conditioned/09-02-SUMMARY.md`
</output>
