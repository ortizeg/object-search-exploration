---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pixi.toml
  - pyproject.toml
  - .pre-commit-config.yaml
  - .gitignore
  - README.md
  - src/object_search/__init__.py
  - src/object_search/py.typed
  - src/object_search/log.py
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_log.py
  - .github/workflows/ci.yml
  - .github/pull_request_template.md
  - .vscode/settings.json
autonomous: true
requirements: [INFRA-01, INFRA-02, INFRA-03, INFRA-04, INFRA-05, INFRA-06, INFRA-07]
user_setup: []

must_haves:
  truths:
    - "pixi install resolves on osx-arm64 with python 3.12, opencv from conda-forge, onnxruntime 1.23.2"
    - "pixi run lint, pixi run typecheck, and pixi run test all exit 0"
    - "A print() statement anywhere in src/ fails pixi run lint"
    - "An import of stdlib logging anywhere in src/ fails pixi run lint"
    - "Coverage below 80 percent fails pixi run test"
    - "CI runs lint, typecheck, and test on every pull request and passes on main"
    - "main is protected: direct pushes are rejected and CI checks are required to merge"
  artifacts:
    - pixi.toml
    - pyproject.toml
    - .pre-commit-config.yaml
    - .github/workflows/ci.yml
    - src/object_search/log.py
    - src/object_search/py.typed
  key_links:
    - "pyproject.toml [tool.ruff.lint] select includes T20 so print() is a lint error"
    - "pyproject.toml [tool.ruff.lint.flake8-tidy-imports] bans stdlib logging"
    - "pyproject.toml [tool.pytest.ini_options] addopts carries --cov-fail-under=80"
    - "pixi.toml [tasks] lint/typecheck/test are the exact commands CI invokes"
---

<objective>
Stand up the repository scaffold and make the three quality gates real: Ruff (line-length
100) with `print()` and stdlib `logging` both banned, MyPy strict, and pytest with an 80%
coverage floor — enforced locally by pre-commit and remotely by CI against a protected
`main`.

Purpose: every subsequent phase inherits these gates. If they are advisory now they will be
advisory forever, and the user has stated they are hard merge gates.
Output: a green `pixi install` / `lint` / `typecheck` / `test`, a green CI run, and branch
protection on `main`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.claude/CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author pixi.toml with the proven version baseline</name>
  <files>pixi.toml</files>
  <read_first>/Users/ortizeg/1Projects/⛹️‍♂️ Next Play/code/basketball-2d-to-3d/pixi.toml, .planning/research/STACK.md</read_first>
  <action>
Use the modern `[workspace]` table (pixi 0.62.x), NOT the legacy `[project]` table.

  [workspace]
  name = "object-search-exploration"
  channels = ["conda-forge"]
  platforms = ["osx-arm64", "linux-64"]

`[dependencies]` (conda-forge — anything with native/binary components):
  python = "3.12.*"
  opencv = ">=4.11"            # conda-forge, NEVER opencv-python-headless
  numpy = ">=2.0"
  scipy = ">=1.14"             # GMM calibration, watershed, connected components
  scikit-learn = ">=1.5"       # GaussianMixture for the gmm calibration strategy
  matplotlib-base = ">=3.9"    # -base avoids pulling a GUI toolkit
  pillow = ">=10"

`[pypi-dependencies]`:
  object-search = { path = ".", editable = true }
  onnxruntime = "==1.23.2"     # exact pin: 1.24.1 breaks the pixi solve on macOS
  pydantic = ">=2.9"
  loguru = ">=0.7"
  fastapi = ">=0.115"
  uvicorn = ">=0.32"
  python-multipart = ">=0.0.12" # required by FastAPI for file upload (API-06)
  hydra-core = ">=1.3"          # CLI/benchmark entrypoint ONLY
  typer = ">=0.12"              # cli.py
  huggingface-hub = ">=0.26"    # fetch-models downloads

`[feature.dev.pypi-dependencies]`: pytest, pytest-cov, ruff, mypy, pre-commit, httpx
(httpx is needed by fastapi's TestClient).

`[environments]`: default = { features = ["dev"], solve-group = "default" }

`[tasks]` — these names are the contract CI invokes, do not rename them:
  lint        = "ruff check src/ tests/"
  format      = "ruff format src/ tests/"
  format-check= "ruff format --check src/ tests/"
  typecheck   = "mypy src/"
  test        = "pytest tests/ -v"
  quality     = { depends-on = ["lint", "format-check", "typecheck", "test"] }
  fetch-models= "python -m object_search.cli fetch-models"
  serve       = "uvicorn object_search.api.app:app --reload --port 8000"
  samples     = "python -m object_search.cli render-samples"
  bench       = "python -m object_search.cli benchmark"
  synth       = "python -m object_search.cli synth"

Pin MINIMUM versions except onnxruntime, which is an exact pin with a comment explaining
why. Let pixi.lock pin the rest.
  </action>
  <verify>pixi install &amp;&amp; pixi list | grep -E "onnxruntime|opencv|numpy|python "</verify>
  <acceptance_criteria>
    - pixi.toml contains '[workspace]' and does NOT contain '[project]'
    - pixi.toml contains 'onnxruntime = "==1.23.2"'
    - pixi.toml contains 'opencv' under [dependencies] and NOT 'opencv-python-headless' anywhere
    - pixi.toml platforms list contains both "osx-arm64" and "linux-64"
    - pixi.lock exists and is committed
    - pixi list shows onnxruntime 1.23.2
  </acceptance_criteria>
  <done>pixi install succeeds and pixi.lock is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Author pyproject.toml as the single source of truth for tooling</name>
  <files>pyproject.toml</files>
  <action>
[project]: name "object-search", version "0.1.0", requires-python ">=3.12", author
Enrique G. Ortiz &lt;ortizeg@gmail.com&gt;. Hatchling build backend with
`[tool.hatch.build.targets.wheel] packages = ["src/object_search"]`.

[tool.ruff]: line-length = 100, target-version = "py312".
[tool.ruff.lint] select = ["E","F","I","N","UP","S","B","A","C4","T20","SIM","ANN","RUF","TID","PTH"].
  - T20 is what makes print() a lint error (INFRA-05).
  - ANN enforces annotations, which keeps mypy strict honest.
[tool.ruff.lint.flake8-tidy-imports.banned-api]: ban "logging" with the message
  "Use loguru: `from loguru import logger`. stdlib logging is banned (INFRA-05)."
  This is the second half of INFRA-05 — T20 alone only catches print().
[tool.ruff.lint.per-file-ignores]:
  "tests/**/*.py" = ["S101", "ANN201", "ANN001"]   # assert is the point of a test
  "src/object_search/cli.py" = ["T20"]  ONLY IF typer requires it — prefer logger and omit
    this ignore entirely. Do not add the ignore speculatively.
[tool.ruff.lint.isort] known-first-party = ["object_search"].

[tool.mypy]: python_version "3.12", strict = true, warn_return_any = true,
  ignore_missing_imports = false (global), plus narrowly scoped overrides:
  [[tool.mypy.overrides]] module = ["cv2.*", "onnxruntime.*", "sklearn.*", "scipy.*"]
    ignore_missing_imports = true
  Do NOT set a global ignore_missing_imports = true, and do NOT set
  disallow_any_explicit = true — Diagnostics payloads legitimately need object-typed
  mappings and the flag would force a pile of ignores that make strict mode dishonest.

[tool.pytest.ini_options]: testpaths ["tests"], addopts
  "--cov=object_search --cov-report=term-missing --cov-report=xml --cov-fail-under=80".
  Note: --cov=object_search (the installed package), not --cov=src.

[tool.coverage.run]: source = ["src/object_search"], and omit the cli entrypoint guard.
  </action>
  <verify>pixi run lint &amp;&amp; pixi run typecheck</verify>
  <acceptance_criteria>
    - pyproject.toml contains 'line-length = 100'
    - pyproject.toml [tool.ruff.lint] select list contains "T20"
    - pyproject.toml contains a banned-api entry for "logging"
    - pyproject.toml contains 'strict = true' under [tool.mypy]
    - pyproject.toml does NOT contain a global 'ignore_missing_imports = true' outside an overrides block
    - pyproject.toml addopts contains '--cov-fail-under=80'
  </acceptance_criteria>
  <done>Both lint and typecheck pass on the empty package.</done>
</task>

<task type="auto">
  <name>Task 3: Package skeleton, py.typed, and the Loguru setup</name>
  <files>src/object_search/__init__.py, src/object_search/py.typed, src/object_search/log.py, tests/__init__.py, tests/conftest.py, tests/test_log.py</files>
  <action>
`src/object_search/py.typed` — empty file (PEP 561).

`src/object_search/__init__.py` — module docstring plus `__version__ = "0.1.0"`.

`src/object_search/log.py` — `setup_logging(level: str = "INFO", log_file: Path | None = None) -> None`
that calls `logger.remove()`, adds a colorized stderr sink, and optionally adds a rotating
file sink (rotation "100 MB", retention "7 days", compression "gz"). Docstring must state
that it is called ONCE at an entry point (CLI main, FastAPI lifespan) and never from a
library module, because configuring sinks inside a library duplicates handlers for every
importer.

`tests/conftest.py` — fixtures used across the suite:
  - `rng` — a seeded `np.random.default_rng(0)` so tests are deterministic
  - `tmp_models_dir` — tmp_path-based dir for inferencer tests
Add a session-scoped autouse fixture that calls `setup_logging("WARNING")` so test output
is not flooded.

`tests/test_log.py` — assert setup_logging removes prior sinks (call twice, assert no
handler duplication by capturing with a sink list), and assert the file sink is created
when log_file is passed.
  </action>
  <verify>pixi run test</verify>
  <acceptance_criteria>
    - src/object_search/py.typed exists
    - src/object_search/log.py contains 'from loguru import logger'
    - src/object_search/log.py does NOT contain 'import logging'
    - grep -rn "print(" src/ returns no matches
    - pixi run test exits 0
  </acceptance_criteria>
  <done>Test suite passes with coverage above the 80% floor.</done>
</task>

<task type="auto">
  <name>Task 4: .gitignore, pre-commit hooks, VS Code settings, README</name>
  <files>.gitignore, .pre-commit-config.yaml, .vscode/settings.json, README.md</files>
  <action>
`.gitignore` — extend the existing file. Must include: `.pixi/`, `__pycache__/`, `*.py[cod]`,
`*.egg-info/`, `dist/`, `build/`, `.coverage`, `coverage.xml`, `htmlcov/`, `.pytest_cache/`,
`.mypy_cache/`, `.ruff_cache/`, `.DS_Store`, and critically:
  models/          # ONNX weights — fetched by `pixi run fetch-models` (INFRA-11)
  *.onnx
  *.pt
  *.pth
  runs.db          # local SQLite store
IMPORTANT: do NOT gitignore `pixi.lock` — this is an application, so the lock is committed.
Do NOT gitignore `docs/samples/` — those renders are committed on purpose (DOC-02).
Do NOT gitignore `assets/demo/` images.

`.pre-commit-config.yaml`:
  - pre-commit/pre-commit-hooks: trailing-whitespace, end-of-file-fixer, check-yaml,
    check-toml, check-added-large-files (args: --maxkb=2048), check-merge-conflict,
    detect-private-key
  - astral-sh/ruff-pre-commit: `ruff` with --fix, and `ruff-format`
  - a local hook `mypy` that runs `pixi run typecheck` with `pass_filenames: false` so it
    type-checks the whole package against the pixi env rather than a hook-local env with
    different dependency versions (mirrors-mypy in its own venv cannot see onnxruntime,
    pydantic, or fastapi and produces false positives)
Then run `pixi run pre-commit install`.

`.vscode/settings.json` — Ruff as formatter, format on save, organize imports on save,
mypy via the pixi env, pytest enabled, and `python.defaultInterpreterPath` pointing at
`.pixi/envs/default/bin/python`.

`README.md` — replace the placeholder. Structure: what it is (one paragraph), quickstart
(`pixi install`, `pixi run fetch-models`, `pixi run serve`), the four methods as a table
with a one-line description each, a "Sample runs" section with a placeholder note that
Phase 2 populates it, project layout, and a Development section listing `pixi run lint /
typecheck / test / quality`. Do not claim anything is implemented that is not.
  </action>
  <verify>pixi run pre-commit run --all-files</verify>
  <acceptance_criteria>
    - .gitignore contains 'models/' and '*.onnx'
    - .gitignore does NOT contain a bare 'pixi.lock' line
    - .pre-commit-config.yaml contains 'detect-private-key' and 'check-added-large-files'
    - .pre-commit-config.yaml mypy hook uses 'pass_filenames: false'
    - .git/hooks/pre-commit exists
    - README.md does not contain the string 'Welcome to your new workspace'
  </acceptance_criteria>
  <done>pre-commit runs clean over all files.</done>
</task>

<task type="auto">
  <name>Task 5: CI workflow and PR template</name>
  <files>.github/workflows/ci.yml, .github/pull_request_template.md</files>
  <action>
`.github/workflows/ci.yml` — triggers on `pull_request` and `push` to `main`.
Single job `quality` on `ubuntu-latest`:
  - actions/checkout@v4
  - prefix-dev/setup-pixi@v0.8.x with `environments: default` and `cache: true`
  - `pixi run lint`
  - `pixi run format-check`
  - `pixi run typecheck`
  - `pixi run test`
  - upload coverage.xml as an artifact
Use `--locked` semantics: setup-pixi should fail if pixi.lock is out of date with pixi.toml,
so the lockfile cannot silently drift. Add `concurrency` with
`group: ${{ github.workflow }}-${{ github.ref }}` and `cancel-in-progress: true`.

Because platforms includes linux-64, the lock covers the CI runner. If setup-pixi reports
the lock does not cover linux-64, fix pixi.toml platforms rather than removing --locked.

`.github/pull_request_template.md` — sections: Summary, Requirements satisfied (a checklist
where each line is a requirement ID and how it was satisfied), Phase success criteria
verified (how each was checked), Test evidence, and Notes/assumptions. This template is
what makes the per-phase PRs readable, which the user explicitly asked for.
  </action>
  <verify>Push the branch and confirm the CI run is green via `gh run list --limit 1` and `gh run view`.</verify>
  <acceptance_criteria>
    - .github/workflows/ci.yml contains 'prefix-dev/setup-pixi'
    - .github/workflows/ci.yml invokes all four of: pixi run lint, format-check, typecheck, test
    - .github/pull_request_template.md contains 'Requirements satisfied'
    - gh run list shows a successful conclusion for the branch
  </acceptance_criteria>
  <done>A CI run completes green.</done>
</task>

<task type="auto">
  <name>Task 6: Branch protection on main (after CI has registered its checks)</name>
  <files>(no repo files — GitHub API only)</files>
  <action>
This task runs LAST, after the CI workflow has completed at least one run, because required
status checks cannot reference a check name GitHub has never seen.

Apply protection to `main` via the GitHub API:
  - require a pull request before merging, 0 required approvals (single-user repo — the
    point is to force the PR flow and the checks, not to fake a review)
  - require the `quality` status check to pass, with strict = true (branch must be up to
    date)
  - dismiss stale approvals, require conversation resolution
  - enforce_admins = false, so the owner can still recover from a broken main without
    fighting the settings

Use `gh api -X PUT repos/ortizeg/object-search-exploration/branches/main/protection` with
the full JSON body. Then verify by reading the protection back and by asserting a direct
push to main is rejected.
  </action>
  <verify>gh api repos/ortizeg/object-search-exploration/branches/main/protection | grep -q "quality"</verify>
  <acceptance_criteria>
    - gh api .../branches/main/protection returns required_status_checks containing "quality"
    - gh api .../branches/main/protection shows required_pull_request_reviews present
    - A direct `git push origin main` is rejected by the remote
  </acceptance_criteria>
  <done>Protection is readable from the API and a direct push to main fails.</done>
</task>

</tasks>

<verification>
- [ ] `pixi install` succeeds on osx-arm64 and `pixi.lock` is committed
- [ ] `pixi run lint` exits 0
- [ ] `pixi run format-check` exits 0
- [ ] `pixi run typecheck` exits 0
- [ ] `pixi run test` exits 0 with coverage ≥80%
- [ ] Adding a temporary `print("x")` to a src file makes `pixi run lint` fail (test it, then remove it)
- [ ] Adding a temporary `import logging` to a src file makes `pixi run lint` fail (test it, then remove it)
- [ ] CI is green on the PR branch
- [ ] `main` rejects a direct push
</verification>

<success_criteria>
- All tasks completed
- Phase 1 success criterion 1 is demonstrably met: lint, typecheck, and test all pass on a
  green CI run against a protected `main`
- No gate is advisory: each of the three has been shown to actually fail when violated
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
