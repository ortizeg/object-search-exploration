---
phase: 05-method-2-sparse-geo
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/object_search/inference/superpoint.py
  - src/object_search/inference/models.py
  - src/object_search/search/sparse_geo.py
  - docs/methods/sparse-geo.md
  - docs/ROBUSTNESS-BACKLOG.md
  - docs/library-reviews/superpoint.md
  - tests/test_superpoint.py
  - tests/test_sparse_geo_sequential.py
autonomous: false
requirements: [METHOD-03, METHOD-04b]
user_setup: []

must_haves:
  truths:
    - "SuperPoint runs through the same backend interface as the classical detectors, switched by config alone"
    - "single-4dof is rejected for the SuperPoint backend (frameless keypoints)"
    - "sequential-RANSAC decomposition is selectable as an alternative to Hough voting"
    - "the SuperPoint library-review verdict is recorded before adoption"
  artifacts:
    - src/object_search/inference/superpoint.py
    - docs/methods/sparse-geo.md
    - docs/library-reviews/superpoint.md
  key_links:
    - "SuperPointInferencer subclasses ONNXInferencer with the verified grayscale contract"
    - "the SuperPoint backend keypoints carry no scale/angle, so single-4dof raises"
---

<objective>
Add the SuperPoint ONNX backend behind the same interface, the sequential-RANSAC alternative,
the method doc, and the sample runs. Human-verified because "Hough peaks visible in the
diagnostics overlay" is a success criterion that needs a person to look at the UI.

Output: `SuperPointInferencer`, the sequential-RANSAC path, docs, sample runs.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
@$HOME/.claude/gsd-core/references/checkpoints.md
</execution_context>

<context>
@.planning/phases/05-method-2-sparse-geo/05-CONTEXT.md
@.claude/CLAUDE.md
@.planning/research/MODELS.md
@src/object_search/inference/onnx_inferencer.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: library-review verdict + fetch-models entry + SuperPointInferencer</name>
  <files>docs/library-reviews/superpoint.md, src/object_search/inference/models.py, src/object_search/inference/superpoint.py, tests/test_superpoint.py</files>
  <read_first>.planning/research/MODELS.md, src/object_search/inference/onnx_inferencer.py</read_first>
  <action>
`docs/library-reviews/superpoint.md` — record the verdict **Trial**: source is the frozen
**v1.0.0 release asset `superpoint.onnx`** from `fabio-sim/LightGlue-ONNX` (the repo's `main` no
longer exports SuperPoint standalone — IDEA.md §14 is stale). Download-only, zero code exposure,
Kornia-validated, but single-maintainer. Licence: MagicLeap **non-commercial research-only**,
DERIVATIVES clause covers the ONNX file — never redistribute; weights stay gitignored.

`models.py` — confirm/add the `superpoint` `ModelSpec` (github-release source, the v1.0.0 asset,
~5.03 MiB, the licence note). Already present from Phase 1 if task 7 populated it; verify.

`superpoint.py` — `SuperPointInferencer(ONNXInferencer)` with the **verified contract** in its
docstring: input `image` f32 `[1,1,H,W]` **grayscale BT.601**, **no mean/std** (scale to [0,1]),
outputs `keypoints` **int64** `(x,y)`, `scores`, `descriptors` **already L2-normalized** (so
kNN is a plain matmul / dot product). Effective border is **8 px**, not the configured 4.
Keep the **variable** keypoint count variable — do not pad to a fixed top-K, because METHOD-04c's
low-keypoint guard reads `keypoints.shape[1]` directly. The returned keypoints carry **no
scale/angle** (None), which is what makes the frameless-mode rejection work.

Tests (model-free where the weight is absent — skip the real-inference test when the file is
missing, keep the contract test model-free): assert the inferencer declares the grayscale
single-channel input spec and that its keypoints expose no scale/angle.
  </action>
  <verify>pixi run test tests/test_superpoint.py -v</verify>
  <acceptance_criteria>
    - docs/library-reviews/superpoint.md records a Trial verdict and the non-commercial licence
    - SuperPointInferencer docstring states the grayscale, no-mean/std, L2-normalized-descriptor contract
    - the backend's keypoints carry no scale/angle
    - the real-inference test skips when the weight file is absent
  </acceptance_criteria>
  <done>SuperPoint loads with its documented contract and is gated by a recorded review.</done>
</task>

<task type="auto">
  <name>Task 2: Backend wiring + single-4dof rejection + config-only switch</name>
  <files>src/object_search/search/sparse_geo.py, tests/test_superpoint.py</files>
  <action>
Add `"superpoint"` to `SparseGeoConfig.backend`. Wire it through the **same** backend boundary
as the classical detectors — the only difference visible to the rest of the module is that its
keypoints have no frame and its descriptors are already normalized. Switching backend is
config alone; no separate code path.

Enforce METHOD-04a: `voting_mode="single-4dof"` with `backend="superpoint"` **raises a clear
error** (frameless keypoints cannot form a single-correspondence similarity). The SuperPoint
default voting mode is `translation-2dof`.

Test: constructing a search with backend=superpoint + single-4dof raises with a message naming
the incompatibility; backend=superpoint + translation-2dof is accepted.
  </action>
  <verify>pixi run test tests/test_superpoint.py -k mode -v</verify>
  <acceptance_criteria>
    - superpoint is a backend option switched by config alone
    - single-4dof + superpoint raises a clear error
    - translation-2dof is the superpoint default
  </acceptance_criteria>
  <done>Both backends share one code path; the invalid mode is refused, not degraded.</done>
</task>

<task type="auto">
  <name>Task 3: Sequential-RANSAC decomposition (METHOD-04b)</name>
  <files>src/object_search/search/sparse_geo.py, tests/test_sparse_geo_sequential.py</files>
  <action>
Add the `decomposition="sequential-ransac"` path as a pluggable alternative to Hough voting,
behind the same interface (it consumes the same correspondences and produces the same list of
accepted models): fit the dominant similarity over all correspondences with the NumPy RANSAC,
remove its inliers, repeat until the inlier count falls below `min_inliers`. Seeded from
`config.seed` like the per-peak path. Mirrors the selectable-strategy pattern in
`search/common/`.

Test: on the 6-instance scene, sequential-ransac recovers multiple models; same seed is
deterministic; it and the Hough path find a comparable instance count.
  </action>
  <verify>pixi run test tests/test_sparse_geo_sequential.py -v</verify>
  <acceptance_criteria>
    - decomposition switches between 'hough' and 'sequential-ransac' by config
    - sequential-ransac recovers multiple models on a 6-instance scene
    - it is deterministic under a fixed seed
  </acceptance_criteria>
  <done>Both decomposition strategies work behind one interface.</done>
</task>

<task type="auto">
  <name>Task 4: docs/methods/sparse-geo.md, robustness backlog, sample runs</name>
  <files>docs/methods/sparse-geo.md, docs/ROBUSTNESS-BACKLOG.md</files>
  <action>
`docs/methods/sparse-geo.md` — headings mirror the numbered steps in the module. Explicit
pre/post-processing for both backends. Config reference generated from the JSON Schema. **Known
Failure Modes** must include the NCC crossover as an **expected finding, not a bug**: when
instances are small and near-identical almost all tentative matches are wrong and Hough's
discriminative power is insufficient — precisely where NCC (Method 1) is strongest, and where
the EVAL-19 chipset benchmark will show the crossover. Mirror the ROBUSTNESS BACKLOG section
(J-linkage/T-linkage; DISK/ALIKED backends and the permissive-licence escape from SuperPoint's
non-commercial terms; post-hoc orientation/scale assignment to unlock single-4dof for
SuperPoint; LoFTR/RoMa as a spike).

Regenerate the sample runs (`pixi run samples`) — the renderer iterates the registry, so
`sparse-geo` appears with no renderer changes; verify that. Append the sparse-geo section to
`docs/ROBUSTNESS-BACKLOG.md`.
  </action>
  <verify>pixi run samples &amp;&amp; grep -q "ROBUSTNESS BACKLOG" docs/methods/sparse-geo.md</verify>
  <acceptance_criteria>
    - docs/methods/sparse-geo.md documents both backends' pre/post-processing
    - the NCC crossover is documented as an expected finding
    - docs/samples/sparse-geo/ is populated by the registry-iterating renderer
    - docs/ROBUSTNESS-BACKLOG.md has a sparse-geo section
  </acceptance_criteria>
  <done>Docs mirror the module and sample runs exist.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>sparse-geo runnable in the app (pixi run serve). Use an image with 6+ textured instances.</what-built>
  <how-to-verify>Pick sparse-geo, draw a box on one textured instance, run Search, and confirm: multiple distinct boxes are returned (not one), and toggling the diagnostics overlay shows the Hough peaks and correspondences. Switch the backend config from sift to superpoint and confirm it still runs through the same path.</how-to-verify>
  <resume-signal>Type "approved" or describe what was wrong.</resume-signal>
</task>

</tasks>

<verification>
- [ ] lint/format-check/typecheck/test green, coverage ≥80%
- [ ] SuperPoint and classical share one path, switched by config
- [ ] single-4dof rejected for SuperPoint
- [ ] Hough peaks visible in the UI (human-verified)
- [ ] sequential-ransac works as an alternative
</verification>

<success_criteria>
- Phase 5 criteria 2, 3, 6 met (peaks in UI, both backends one path, low-keypoint diagnostic)
</success_criteria>

<output>
After completion, create `.planning/phases/05-method-2-sparse-geo/05-02-SUMMARY.md`
</output>
