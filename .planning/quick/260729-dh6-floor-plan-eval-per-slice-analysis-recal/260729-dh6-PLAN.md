---
quick_id: 260729-dh6
type: execute
autonomous: true
requirements: [EVAL-10, EVAL-23, EVAL-24]
files_modified:
  - src/object_search/eval/metrics.py
  - src/object_search/eval/benchmark.py
  - src/object_search/eval/tuning.py
  - src/object_search/eval/sampling.py
  - src/object_search/search/dino_dense.py
  - src/object_search/inference/dinov2.py
  - docs/methods/dino-dense.md
  - tests/test_eval_metrics_coco.py
  - tests/test_research_floorplans.py
  - tests/test_eval_tuning.py
  - tests/test_dino_dense.py
must_haves:
  truths:
    - "The research benchmark block carries a `slices` section (symbol-size recall, crowding F1, plan-resolution F1) that flows through run_research_sweep per cell."
    - "match_predictions still returns (tp, fp, fn) unchanged; a sibling returns which GT indices were matched."
    - "Tuning grids are broadened (symbol-matched scales + wider retain_frac + a 2nd knob) and readable/explicit."
    - "dino-dense can run every floor plan through ONE fixed onnxruntime input shape; the default path stays byte-identical on chipset/synthetic."
  artifacts:
    - src/object_search/eval/metrics.py
    - src/object_search/eval/benchmark.py
    - src/object_search/eval/tuning.py
    - src/object_search/search/dino_dense.py
  key_links:
    - "benchmark._run_one_research -> metrics matched-GT sibling -> per-image gt-box records -> research slices"
    - "tuning grids -> spec.config_model(**overrides) (multi-key dicts already validated per method)"
---

<objective>
Enrich the floor-plan evaluation (Roboflow floor-plans-500; keys `floorplans-door` /
`floorplans-window`) with per-slice result analysis, broadened domain threshold tuning, and a
`dino-dense` GPU-OOM fix. Code + model-free tests only — the GPU sweep itself is a manual
vast.ai step run AFTER this lands.

Purpose: turn one pooled floor-plan number into an actionable per-method, per-slice picture, let
each method be tuned harder to the domain, and stop `dino-dense` OOMing on the varied-resolution
plans.
Output: additive slices on the research path, wider tuning grids, an opt-in fixed-size letterbox
in the DINOv2 dense path, and model-free (`ncc`, no weights) tests for all three.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@src/object_search/eval/metrics.py
@src/object_search/eval/benchmark.py
@src/object_search/eval/tuning.py
@src/object_search/eval/labels.py
@src/object_search/search/dino_dense.py
@src/object_search/inference/dinov2.py
@tests/test_research_floorplans.py
@tests/test_eval_tuning.py
@tests/test_eval_metrics_coco.py
</context>

<constraints>
- Hard gates on every task: `pixi run lint` (ruff-100), `pixi run typecheck` (mypy strict),
  `pixi run test` (>=80% coverage floor). Loguru only, no `print()`. Frozen Pydantic for any new
  inter-layer model.
- `pixi` is a shell function in this environment — invoke the binary directly as
  `$HOME/.pixi/bin/pixi run <task>` from the shell.
- Prefer additive / backward-compatible changes. Existing tests must stay green with no edits
  except where a task explicitly extends them.
- Do NOT run `bench-research` / `tune-floorplans` on real data, do NOT provision vast.ai, do NOT
  edit `scripts/gpu_bench.sh`. Real-data result JSON stays gitignored.
- Each task is independently committable. Tests live WITH their task (each task ends green),
  per the repo's coverage-floor discipline — there is no separate "tests" task.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: matched-GT-index sibling in metrics.py (backward-compatible)</name>
  <files>src/object_search/eval/metrics.py, tests/test_eval_metrics_coco.py</files>
  <behavior>
    - Existing `match_predictions(pred, gt, iou)` returns `(tp, fp, fn)` byte-for-byte unchanged;
      all current callers/tests stay green.
    - New sibling (e.g. `match_predictions_detailed`) reuses the SAME greedy `_best_unmatched_gt`
      loop and EVAL-16 duplicate rule, and additionally returns which GT indices were matched
      (e.g. a `tuple[bool, ...]` aligned to `gt_boxes`, or the matched index set). `tp` equals the
      number of matched GT; the abstention/duplicate semantics are identical to the tuple form.
    - `match_predictions` is re-expressed in terms of the sibling (single matching implementation),
      so the two can never drift.
  </behavior>
  <action>
    Add a detailed matcher to metrics.py that returns, alongside the counts, the per-GT matched
    flags, reusing the existing `_best_unmatched_gt` helper so the greedy highest-IoU,
    match-each-GT-at-most-once rule is written exactly once. Keep the original
    `match_predictions` public signature and return type; have it delegate to the sibling and
    project to `(tp, fp, fn)`. Document the sibling's return contract in its docstring (which GT
    indices, alignment to `gt_boxes`, that `sum(matched) == tp`). No behavior change to counts.
    Extend tests to assert: (a) the tuple form is unchanged on the existing fixtures, (b) the
    sibling reports the correct matched indices including the EVAL-16 duplicate case (two preds on
    one GT -> that GT matched once, second pred not credited).
  </action>
  <verify>
    <automated>$HOME/.pixi/bin/pixi run test -- tests/test_eval_metrics_coco.py -q</automated>
  </verify>
  <done>
    Sibling returns matched-GT flags; `match_predictions` return contract unchanged; new tests pass
    and lint/typecheck clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: per-slice research metrics wired through benchmark + sweep</name>
  <files>src/object_search/eval/benchmark.py, tests/test_research_floorplans.py</files>
  <behavior>
    - `_run_one_research` uses the Task-1 detailed matcher and records, per GT box, its
      symbol-size bucket and whether it was matched (an additive, JSON-serializable field on
      `ImageResult`, default empty so the chipset/CI path stays byte-identical).
    - `run_research_benchmark` returns an additive `slices` block with three groupings:
      * `by_symbol_size` — RECALL per small/medium/large bucket, bucketed by GT-box-area /
        plan-area (plan-area = `gt.width * gt.height`); pooled over ALL GT boxes across the cell's
        images (a GT-box-level aggregation, not `_slice_by`).
      * `by_crowding` — F1 per instances-per-plan bucket (bucket `instance_count`, reuse `_slice_by`).
      * `by_plan_resolution` — F1 per canvas-size bucket (bucket `canvas_size`, reuse `_slice_by`).
    - The per-cell block in `run_research_sweep` carries the same `slices` (currently it emits only
      `coverage` + `overall`).
  </behavior>
  <action>
    Add an additive frozen field to `ImageResult` holding the per-GT-box records for the research
    path (each: size bucket string + matched bool). Populate it in `_run_one_research` from the
    Task-1 detailed matcher (map each GT box's area / (width*height) to an explicit
    small/medium/large cut — choose readable, documented thresholds and state them in the
    docstring; guard missing width/height by skipping the box from the size aggregation, never
    defaulting). Add a `_recall_by_size(records)` helper that pools matched/total across every GT
    record and emits per-bucket recall with the abstention convention (recall `None` when a bucket
    has zero GT). Add coarse bucket functions for crowding (instances-per-plan) and plan-resolution
    (canvas long-side); reuse `_slice_by` for those two so they share the micro-averaged
    `_aggregate_research` pooling. Assemble the `slices` block in `run_research_benchmark` and
    include it in each `run_research_sweep` cell. Keep chipset `run_benchmark` untouched. Extend
    the floor-plans tests (model-free `ncc`, the existing fixture) to assert the `slices` block has
    all three groupings, that `by_symbol_size` values are recalls in `[0,1]` or `None`, and that a
    sweep cell carries `slices`.
  </action>
  <verify>
    <automated>$HOME/.pixi/bin/pixi run test -- tests/test_research_floorplans.py tests/test_eval_benchmark.py -q</automated>
  </verify>
  <done>
    Research block + sweep cells carry `by_symbol_size` / `by_crowding` / `by_plan_resolution`;
    chipset path byte-identical; model-free tests pass; lint/typecheck clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: broadened tuning grids + exemplar-count and size-representative options</name>
  <files>src/object_search/eval/tuning.py, src/object_search/eval/sampling.py, tests/test_eval_tuning.py</files>
  <behavior>
    - `_TUNING_GRIDS` broadened, staying readable/explicit: `ncc`/`mosse` get symbol-matched
      `scales` sets + wider `retain_frac` + a 2nd knob (`nms_iou` or `calibration`);
      `sparse-geo` and `propose-retrieve` get a wider primary knob + a 2nd knob (`nms_iou`);
      `owlv2-oneshot` gets `max_box_area_frac` + `query_iou_frac`. Multi-key override dicts already
      validate through `spec.config_model(**overrides)`, so `tune_method`'s loop is unchanged.
      Field names are the real frozen-config fields (verified: ncc/mosse `scales`,`retain_frac`,
      `nms_iou`,`calibration`; sparse-geo `min_inliers`,`nms_iou`; propose-retrieve
      `similarity_floor`,`nms_iou`; owlv2 `max_box_area_frac`,`query_iou_frac`,`retain_frac`).
    - An additive exemplar-selection mode ("size-representative": pick the exemplar whose box area
      is closest to the per-image median box area) is threaded from tuning through
      `run_research_benchmark` -> `_run_one_research` -> the sampler, default `"seeded-random"`
      preserving current behaviour byte-for-byte.
    - `run_domain_tuning` can tune/report at exemplar_count {1, 3}; the default output for a single
      count matches the committed report shape.
  </behavior>
  <action>
    Widen `_TUNING_GRIDS` using explicit nested tuple/comprehension construction with a comment per
    method naming the two knobs and why each trades recall vs precision (mirror the existing grid
    comments). For `ncc`/`mosse` include a small set of symbol-matched `scales` tuples (floor-plan
    door/window symbols are small and near-fixed-scale — bias the sets tighter/finer than the
    method default) crossed with the wider `retain_frac` range. Add an additive
    `exemplar_selection` param (a `Literal`) to the sampler entry in sampling.py implementing the
    size-representative pick (median box area, deterministic tie-break by index — no new RNG), and
    thread it as an additive keyword (default `"seeded-random"`) through `run_research_benchmark`,
    `_run_one_research`, `tune_method`, and `run_domain_tuning`. Add exemplar-count support to
    `run_domain_tuning` as an additive `exemplar_counts` sequence (default `(1,)` -> unchanged
    single-count report; multiple counts nest per-count blocks). Extend the model-free tuning tests
    to cover: a broadened multi-knob grid runs and selects an in-grid argmax, the
    size-representative selection is deterministic and picks the median-area box, and a
    2-count `run_domain_tuning` produces one block per count. Keep everything offline (`ncc`).
  </action>
  <verify>
    <automated>$HOME/.pixi/bin/pixi run test -- tests/test_eval_tuning.py -q</automated>
  </verify>
  <done>
    Grids broadened and readable; size-representative + {1,3} options additive with
    behaviour-preserving defaults; tuning tests pass; lint/typecheck clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: dino-dense fixed-size letterbox (opt-in) GPU-OOM fix</name>
  <files>src/object_search/search/dino_dense.py, src/object_search/inference/dinov2.py, docs/methods/dino-dense.md, tests/test_dino_dense.py</files>
  <behavior>
    - New additive `DinoDenseConfig` field (e.g. `fixed_input_side: int | None = None`,
      `ge=DINOV2_PATCH`, validated to be a multiple of 14). `None` = current native/cap path,
      byte-identical on chipset and synthetic (no regression).
    - When set, every scene is letterboxed into ONE fixed square input shape (side =
      `fixed_input_side`, a multiple of 14) via a single uniform aspect-preserving scale + a
      constant bottom-right pad, so onnxruntime sees a single input resolution across
      varied-size plans (the CUDA arena is allocated once — the OOM fix).
    - Padding tokens cannot produce matches: token positions whose patch centre lands in the padded
      region are excluded from the similarity map before thresholding / connected components.
    - Boxes map back to original pixels through the single uniform letterbox scale (bottom-right
      pad => zero offset). Determinism preserved (deterministic resize + constant pad).
    - Preprocessing documented in the module docstring "Pre-processing (exact)" section AND
      docs/methods/dino-dense.md; the DINOv2Inferencer docstring cross-references the fixed-size
      letterbox option (its snap-to-multiple(14) contract is a no-op on an already-multiple-of-14
      letterboxed input, so the inferencer contract is unchanged).
  </behavior>
  <action>
    Add the `fixed_input_side` field to `DinoDenseConfig` with a Field validator rejecting a value
    that is not a multiple of `DINOV2_PATCH`, and a description (it drives the UI form). In
    `search`, after the existing `scene_max_side` cap step, when `fixed_input_side` is set build a
    fixed-side letterbox: compute one uniform scale that fits the (capped) scene inside the square,
    resize, and pad bottom-right to exactly `fixed_input_side x fixed_input_side` with a constant
    value; carry the content pixel extent so padding tokens are masked. Run `dense_tokens` on the
    fixed canvas, then set the similarity of any token whose patch centre falls outside the content
    extent to a value below any threshold (so connected components fire only in content). Map
    accepted/candidate boxes back to original pixels with the single letterbox scale. Keep the
    `None` path exactly as today (do not perturb the cap/native branch) so chipset/synthetic stay
    byte-identical. Document the fixed-size letterbox path (scale, constant pad, padding-token mask,
    inverse map) in the module docstring and docs/methods/dino-dense.md; add a one-line
    cross-reference to it in the DINOv2Inferencer docstring. Add model-free tests (pure-numpy
    helpers, no weights): the letterbox+mask helper maps a known box back to original pixels
    correctly, padding tokens are masked below threshold, and a multiple-of-14 validator rejects a
    non-multiple `fixed_input_side`; assert the `None` default leaves the existing helper outputs
    unchanged.
  </action>
  <verify>
    <automated>$HOME/.pixi/bin/pixi run test -- tests/test_dino_dense.py -q</automated>
  </verify>
  <done>
    `fixed_input_side` gives one onnxruntime input shape for all plans; default path byte-identical;
    padding masked; docstrings + method doc updated; model-free tests pass; lint/typecheck clean.
  </done>
</task>

</tasks>

<verification>
Whole-suite gates after all four tasks:
- `$HOME/.pixi/bin/pixi run lint`
- `$HOME/.pixi/bin/pixi run typecheck`
- `$HOME/.pixi/bin/pixi run test` (>=80% coverage floor holds; full suite green, no weights)
</verification>

<success_criteria>
- Research benchmark + sweep expose `by_symbol_size` (recall), `by_crowding` (F1), and
  `by_plan_resolution` (F1); chipset path byte-identical.
- `match_predictions` return contract unchanged; a sibling exposes matched-GT indices.
- Tuning grids broadened per method with real config fields; size-representative exemplar and
  exemplar_count {1,3} available as additive, behaviour-preserving options.
- `dino-dense` runs all floor plans through one fixed onnxruntime input shape when opted in; the
  default path does not regress synthetic/chipset results; preprocessing documented.
- Ruff-100, mypy-strict, and the >=80% coverage floor all green with no fetched weights.
</success_criteria>

<output>
Commit each task atomically. No SUMMARY file required for this quick task beyond the commit trail;
STATE is updated by the quick-task orchestrator.
</output>
