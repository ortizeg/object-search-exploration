---
phase: quick-260810-h6e
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/MILESTONE-2.md
  - docs/explorations/marker-conditioned.md
  - docs/methods/dino-dense.md
  - docs/methods/mosse.md
  - docs/methods/ncc.md
  - docs/methods/propose-retrieve.md
  - docs/eval/research-datasets.md
autonomous: true
requirements: [DOC-QUICK-260810-h6e]

must_haves:
  truths:
    - "`mkdocs build --strict` exits 0 and emits none of the 7 previously-reported broken-link INFO lines."
    - "Six `docs/samples/<name>/` directory links resolve to the real `index.md` page in the built site."
    - "The `research-datasets.md` in-page Scope link resolves to the real `#scope-exclusions` anchor."
    - "No visible link text, prose, image link, or adjacent `index.md` link changed anywhere."
  artifacts:
    - docs/MILESTONE-2.md
    - docs/explorations/marker-conditioned.md
    - docs/methods/dino-dense.md
    - docs/methods/mosse.md
    - docs/methods/ncc.md
    - docs/methods/propose-retrieve.md
    - docs/eval/research-datasets.md
  key_links:
    - "Each edited directory link now points at the `index.md` file mkdocs actually builds, not at the directory."
    - "The Scope anchor matches the slug mkdocs-material generates for `## Scope — exclusions` (`scope-exclusions`, em-dash collapsed to a single hyphen)."
---

<objective>
Fix the 7 broken/incorrect internal markdown links that `mkdocs build --strict` reports as
"unrecognized relative link" / "no such anchor" INFO lines.

Purpose: the strict docs build currently emits avoidable link-warning noise, which hides real
regressions and produces links that 404 in the published site.
Output: 7 docs/*.md files, one link target changed in each. No other content changes.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md

This task is already fully diagnosed. Every target below was verified on disk before planning:
- All six `docs/samples/<name>/index.md` files exist.
- `docs/eval/research-datasets.md` line 237 is `## Scope — exclusions`, and the built HTML for
  that heading carries `id="scope-exclusions"` — mkdocs-material's slugifier collapses the
  em-dash to a **single** hyphen, so the existing double-hyphen link was simply wrong.

Do NOT do further discovery, do NOT restructure the pages, and do NOT touch `mkdocs.yml`.

Shell note: `pixi` is a shell function in the user's interactive shell and is NOT on PATH in a
non-interactive Bash tool call. Invoke it as `$HOME/.pixi/bin/pixi run ...`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repoint the six samples-directory links at their index.md</name>
  <files>docs/MILESTONE-2.md, docs/explorations/marker-conditioned.md, docs/methods/dino-dense.md, docs/methods/mosse.md, docs/methods/ncc.md, docs/methods/propose-retrieve.md</files>
  <action>
Make exactly six single-link edits. In each case change ONLY the URL inside the parentheses of
that one markdown link — append `index.md` to the directory path. The visible link text (the
backticked `docs/samples/...` string) is unchanged in all six.

1. docs/MILESTONE-2.md line 11 — target `samples/marker-conditioned/` becomes `samples/marker-conditioned/index.md` (this page is at the docs root, so the path has no `../` prefix).
2. docs/explorations/marker-conditioned.md line 175 — target `../samples/marker-conditioned/` becomes `../samples/marker-conditioned/index.md`.
3. docs/methods/dino-dense.md line 293 — target `../samples/dino-dense/` becomes `../samples/dino-dense/index.md`.
4. docs/methods/mosse.md line 277 — target `../samples/mosse/` becomes `../samples/mosse/index.md`.
5. docs/methods/ncc.md line 221 — target `../samples/ncc/` becomes `../samples/ncc/index.md`.
6. docs/methods/propose-retrieve.md line 233 — target `../samples/propose-retrieve/` becomes `../samples/propose-retrieve/index.md`.

Use Edit with an old_string long enough to be unique on the page (include surrounding words from
the same line), so the edit cannot land on a neighbouring link.

Leave every other `samples/...` reference on these pages alone. Specifically: the adjacent
`[index.md](../samples/.../index.md)` links and the per-image `![...](.../foo.png)` links already
resolve correctly and must not be touched. Do not reflow, rewrap, or reword any line.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; test "$(grep -rEn '\]\((\.\./)?samples/[a-z0-9-]+/\)' docs/ | wc -l | tr -d ' ')" = "0" &amp;&amp; test "$(git diff --numstat -- docs/ | awk '$1!=$2 || $1>1 {print}' | wc -l | tr -d ' ')" = "0" &amp;&amp; echo PASS</automated>
  </verify>
  <done>No markdown link in docs/ targets a bare `samples/&lt;name&gt;/` directory any more, and `git diff --numstat` shows exactly 1 line added / 1 line removed per touched file (no collateral edits).</done>
</task>

<task type="auto">
  <name>Task 2: Fix the research-datasets Scope anchor</name>
  <files>docs/eval/research-datasets.md</files>
  <action>
On line 15, change the in-page link target from `#scope--exclusions` (double hyphen) to
`#scope-exclusions` (single hyphen), so it matches the `id="scope-exclusions"` anchor
mkdocs-material generates for the `## Scope — exclusions` heading at line 237.

Change only that fragment. The visible link text `Scope` and the surrounding sentence stay as-is.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; grep -q '(#scope-exclusions)' docs/eval/research-datasets.md &amp;&amp; ! grep -q 'scope--exclusions' docs/eval/research-datasets.md &amp;&amp; echo PASS</automated>
  </verify>
  <done>`docs/eval/research-datasets.md` contains `(#scope-exclusions)` and contains no occurrence of `scope--exclusions`.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the strict build is clean and commit</name>
  <files>(no files modified — verification and commit only)</files>
  <action>
Run the strict docs build and capture its full output:

    $HOME/.pixi/bin/pixi run docs-build

Assert both of the following:
1. Exit code is 0.
2. None of the 7 fixed links appear in any "contains an unrecognized relative link" /
   "contains a link ... but there is no such anchor" INFO line. Grep the captured output for
   `unrecognized relative link` and for `no such anchor` and confirm no remaining hit names any
   of: `samples/marker-conditioned/`, `samples/dino-dense/`, `samples/mosse/`, `samples/ncc/`,
   `samples/propose-retrieve/`, or `scope--exclusions`.

Expected remaining output: a single orphan-page-style INFO line about
`benchmark/floorplans-results.md`. That file is a gitignored, untracked local artifact and is a
known, accepted state — it is OUT OF SCOPE. Do not touch it and do not touch `mkdocs.yml` to
silence it.

If the build surfaces any NEW warning not present before, stop and report rather than papering
over it.

Then make ONE commit containing only the 7 docs/*.md files.

Before committing, confirm `git rev-parse --show-toplevel` ends in `object-search-exploration`
(the parent directory is a stray git repo — never commit there).

Commit message:

    docs: fix 7 broken internal links flagged by strict mkdocs build

with the body noting the six samples-directory links repointed at `index.md` and the
`#scope-exclusions` anchor correction, and ending with:

    Co-Authored-By: Claude Opus 4.8 (1M context) &lt;noreply@anthropic.com&gt;
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; $HOME/.pixi/bin/pixi run docs-build 2>&amp;1 | tee /tmp/h6e-docs-build.log; test "${PIPESTATUS[0]}" = "0" &amp;&amp; ! grep -E 'unrecognized relative link|no such anchor' /tmp/h6e-docs-build.log | grep -qE 'samples/(marker-conditioned|dino-dense|mosse|ncc|propose-retrieve)/|scope--exclusions' &amp;&amp; test "$(git show --stat --name-only --format= HEAD | grep -c '^docs/')" = "7" &amp;&amp; echo PASS</automated>
  </verify>
  <done>`mkdocs build --strict` exits 0, no build-output line mentions any of the 6 samples directories or `scope--exclusions` as a broken link/anchor, and HEAD is a single commit touching exactly 7 files, all under `docs/`.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only change. No code path, no user input, no dependency, no network, no runtime surface is touched. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-h6e-01 | Tampering | Collateral edits to docs pages beyond the 7 intended links | low | mitigate | Task 1 verify gate asserts `git diff --numstat` is exactly 1 added / 1 removed line per file; Task 3 asserts the commit touches exactly 7 `docs/` files. |
| T-h6e-02 | Information Disclosure | Published site links | low | accept | Targets are already-committed public docs pages; repointing a link at an existing page exposes nothing new. |
</threat_model>

<verification>
1. `$HOME/.pixi/bin/pixi run docs-build` exits 0.
2. Build output contains no "unrecognized relative link" / "no such anchor" line naming any of the
   six samples directories or `scope--exclusions`.
3. `git show --stat HEAD` lists exactly 7 files, all `docs/*.md`, each with a 1-line change.
4. `mkdocs.yml` and `docs/benchmark/floorplans-results.md` are untouched.
</verification>

<success_criteria>
- All 7 broken-link INFO lines are gone from the strict build output.
- The strict build still exits 0.
- Exactly 7 docs files changed, one link target each; no prose, link text, image link, or
  adjacent correct `index.md` link altered.
- One atomic commit, docs-only.
</success_criteria>

<output>
Create `.planning/quick/260810-h6e-fix-broken-relative-links-flagged-by-str/260810-h6e-SUMMARY.md` when done
</output>
