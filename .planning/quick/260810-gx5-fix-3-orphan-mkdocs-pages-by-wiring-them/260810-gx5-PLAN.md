---
phase: quick-260810-gx5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - mkdocs.yml
autonomous: true
requirements:
  - QUICK-260810-gx5
must_haves:
  truths:
    - "A reader browsing the rendered docs site can reach the DINO-dense floor-plan improvement report from the Improvement reports nav section without following an inline link from another page."
    - "A reader browsing the rendered docs site can reach the OWLv2 floor-plan improvement report from the Improvement reports nav section."
    - "A reader browsing the rendered docs site can reach the floor-plan benchmark results page from the Evaluation nav section."
    - "`pixi run docs-build` still succeeds under `strict: true`, and its 'pages exist in the docs directory but are not included in the nav configuration' listing no longer names any of the three pages."
  artifacts:
    - mkdocs.yml
  key_links:
    - "Each new nav value is a docs_dir-relative path that resolves to an existing file — a typo would fail the strict build outright."
    - "Nav labels follow the existing naming convention of the section they are inserted into, so the site menu stays internally consistent."
---

<objective>
Wire three already-written, already-cross-linked docs pages into `mkdocs.yml`'s `nav:` block so
they are reachable from the site navigation menu rather than only via inline links from other
pages.

Purpose: `pixi run docs-build` reports three orphan pages. They are real, complete, and linked
from ROBUSTNESS-BACKLOG.md, methods/owlv2-oneshot.md, methods/dino-dense.md, and
eval/floorplans-findings.md — but a reader who never clicks one of those inline links cannot
find them at all.

Output: one `mkdocs.yml` edit (nav only), one commit.

Explicitly OUT OF SCOPE (a separate follow-up task owns these — do not touch them):
- The INFO warnings about broken relative links to `samples/*/`.
- The missing anchor in `eval/research-datasets.md`.
- Any change to the three docs pages themselves; their content is already correct.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@mkdocs.yml
@.claude/CLAUDE.md
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Add the three orphan pages to the mkdocs nav</name>
  <files>mkdocs.yml</files>
  <precondition>`git rev-parse --show-toplevel` ends in `object-search-exploration` (the parent directory is a stray empty git repo covering every sibling project — never operate there).</precondition>
  <action>
Edit ONLY the `nav:` block of `mkdocs.yml`. Three insertions, no deletions, no reordering of
existing entries, no changes anywhere else in the file.

Under the existing `Improvement reports:` section, whose current entries are (in order) NCC,
MOSSE, Sparse-geo, DINO dense, OWLv2, OWLv2 floor-plan fine-tune, OWLv2 (real-objects),
Propose–retrieve:

1. Immediately after the `DINO dense` entry, insert an entry pointing at
   `reports/dino-dense-floorplans-improvement.md`, labelled `"DINO dense (floor-plans)"`.
2. Immediately after the `OWLv2` entry and BEFORE the `OWLv2 floor-plan fine-tune` entry,
   insert an entry pointing at `reports/owlv2-floorplans-improvement.md`, labelled
   `"OWLv2 (floor-plans)"`. Ordering rationale: the improvement log precedes the fine-tune
   experiment chronologically, and this keeps all three OWLv2 floor-plan-adjacent entries
   contiguous.

Under the existing `Evaluation:` section:

3. Immediately BEFORE the `Floor-plan findings` entry, insert an entry pointing at
   `benchmark/floorplans-results.md`, labelled `"Floor-plan results"`. Ordering rationale:
   this puts the target-domain results page directly next to the findings page that
   interprets it, in the same results-then-findings order the section already uses.

Label conventions to honour: the parenthetical-qualifier form (`OWLv2 (real-objects)`) is
already established in the Improvement reports section, so the two new report labels match it.
Quote the two labels containing parentheses, matching how existing quoted labels in the file
are written. Nav values are `docs_dir`-relative (the file sets `docs_dir: docs`), so write
`reports/...` and `benchmark/...`, NOT `docs/reports/...`.

All three target files already exist on disk; this task creates no Markdown.
  </action>
  <verify>
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; python -c "import yaml,sys; n=yaml.safe_load(open('mkdocs.yml').read().replace('!!python/name:','#'))['nav']; s=str(n); missing=[p for p in ('reports/dino-dense-floorplans-improvement.md','reports/owlv2-floorplans-improvement.md','benchmark/floorplans-results.md') if p not in s]; sys.exit('MISSING FROM NAV: '+repr(missing) if missing else 0)"</automated>
  </verify>
  <done>`mkdocs.yml`'s parsed `nav` structure contains all three page paths; `git diff --stat` shows `mkdocs.yml` as the only changed file, with additions only.</done>
  <reversibility rating="reversible">A nav-only YAML edit; revert is a one-line `git checkout`.</reversibility>
</task>

<task type="auto">
  <name>Task 2: Prove the strict build is green and the orphan listing no longer names these pages</name>
  <files>mkdocs.yml</files>
  <precondition>`pixi` is a shell function in interactive shells and is NOT on `PATH` for non-interactive tool invocations — call the binary directly as `$HOME/.pixi/bin/pixi`.</precondition>
  <action>
Run the mkdocs strict build and capture its combined stdout+stderr to a log, then assert two
things about that log: the build exited zero (strict mode is on, so a bad nav path or broken
internal link fails hard), and the block mkdocs emits listing pages that exist under `docs/`
but are absent from the nav configuration no longer names any of the three pages from Task 1.

Read the captured log before declaring success. If the build fails, the near-certain cause is a
mistyped nav path in Task 1 — fix the path, do not weaken `strict`, and do not add the file to
an exclusion list.

Pre-existing INFO diagnostics in that same log — the relative-link warnings pointing at the
sample-run directories, and the unresolved anchor in the research-datasets page — are expected
and owned by a separate follow-up task. Their continued presence is NOT a failure of this task
and must not be "fixed" here.

Then commit. The diff must be `mkdocs.yml` alone. Use a `docs:`-scoped conventional commit
message naming what was wired in, and end the message with the `Co-Authored-By:` trailer
required by the repo conventions in `.claude/CLAUDE.md`.
  </action>
  <verify>
    <!-- planner-discipline-allow: floorplans-improvement, floorplans-results -->
    <!-- The negative grep below is scoped to captured build OUTPUT (/tmp/gx5-docs-build.log), never to repo source, so no comment or prose in this plan can satisfy or invalidate it. -->
    <automated>cd "$(git rev-parse --show-toplevel)" &amp;&amp; $HOME/.pixi/bin/pixi run docs-build &gt;/tmp/gx5-docs-build.log 2&gt;&amp;1 &amp;&amp; ! grep -A 20 'not included in the nav configuration' /tmp/gx5-docs-build.log | grep -E 'floorplans-improvement|floorplans-results' &amp;&amp; test "$(git diff --name-only HEAD~1 HEAD)" = "mkdocs.yml"</automated>
  </verify>
  <done>`pixi run docs-build` exits 0 under `strict: true`; the orphan-page listing in its output names none of the three pages; exactly one new commit exists and it touches `mkdocs.yml` only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo → published GitHub Pages site | CI builds `site/` from this config and publishes it publicly; nav decides what is surfaced to anonymous readers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-gx5-01 | Information disclosure | `mkdocs.yml` nav → public Pages site | low | accept | All three pages are already committed under `docs/`, already cross-linked from in-nav pages, and already published in `site/`. Adding a menu entry changes discoverability, not exposure. Contents are method-evaluation logs with no credentials, no weights, and no private data. |
| T-gx5-02 | Denial of service | CI docs-build job | low | mitigate | A bad nav path breaks the `strict` build and would block the Pages deploy. Task 2 runs the identical `pixi run docs-build` locally and requires exit 0 before the commit lands. |
| T-gx5-SC | Tampering | npm/pip/cargo installs | low | accept | No package installs of any kind in this plan; no dependency graph change. No legitimacy audit required. |
</threat_model>

<verification>
1. `$HOME/.pixi/bin/pixi run docs-build` exits 0 with `strict: true` unchanged.
2. The build's orphan-page listing names none of the three pages wired in.
3. `git diff` for the commit shows `mkdocs.yml` only, additions only — no deletions, no
   reordering of pre-existing nav entries.
4. Nav labels for the two new reports match the parenthetical-qualifier convention already
   used in the Improvement reports section.
</verification>

<success_criteria>
- Three nav entries added; all three previously-orphaned pages reachable from the site menu.
- Strict mkdocs build green.
- One commit, `mkdocs.yml` only, with the repo's required `Co-Authored-By:` trailer.
- Out-of-scope INFO diagnostics (sample-run relative links, research-datasets anchor) left
  exactly as they were.
</success_criteria>

<output>
Create `.planning/quick/260810-gx5-fix-3-orphan-mkdocs-pages-by-wiring-them/260810-gx5-SUMMARY.md` when done.
</output>
